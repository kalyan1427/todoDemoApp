import { SearchByStatusPipe } from './search-by-status.pipe';

describe('SearchByStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
